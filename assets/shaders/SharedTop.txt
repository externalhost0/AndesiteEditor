struct VSInput {
    uint VertexID : SV_VertexID;
};
struct FSOutput {
    float4 FragColor : SV_Target0;
};
struct v2f {
    // shader predefined
    float4 ClipPos : SV_Position;
    // defined by me
    float3 Normal           : NORMAL;
    float2 UV               : TEXCOORD0;
    float3 WorldPosition    : POSITION;
};
struct Vertex {
    float3 position;
    float uv_x;
    float3 normal;
    float uv_y;
    float4 tangent;
};

struct Camera {
    float4x4 proj;
    float4x4 view;
    float3 position;
}
struct PerFrameData {
    Camera camera;
    float time;
    float2 resolution;
}

float4 PBR(float3 albedo, float metallic, float roughness) {

    return float4(albedo, 1.0);
}
